{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% set users = [{name: 'John'}, {name: 'Rubs'}] %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .flash-notice { background-color: green }
    .flash-warning { background-color: orange }
</style>

<div class="example-wrapper">
    <h1>Hola {{ controller_name|upper }}! ✅</h1>
    Este mensaje esta viniendo de:
    <ul>
        <li>Tu controlador en <code><a href="{{ 'C:/xampp/htdocs/symfony-curso/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Tu plantilla en <code><a href="{{ 'C:/xampp/htdocs/symfony-curso/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>

    {% for user in users | filter(user => user.name == 'Rubs') %}
        <li> {{user.name}} </li>
    {% endfor %}

    {{ include("includes/subview.html.twig", { 'users': users }) }}

    {% for i in 1..10 %}
        <div class="{{ cycle(['even', 'odd'], i) }}">
            {{i}}
        </div>
    {% endfor %}

    {# <ul>
        {% for user in users %}
            <li>{{user.name}} - you won {{randomGift[loop.index0]}}</li>
        {% endfor %}
    </ul> #}

    {# {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="flash-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %} #}

</div>
{% endblock %}
